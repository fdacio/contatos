
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    content: {
        flexDirection: 'row',
    },
    button: {
        marginLeft: 16
    }
});

export default styles;