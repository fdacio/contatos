
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    content: {
        flexDirection: 'row',
    },

    button: {
        padding: 8,
        width: 40,
        height: 40,
        alignItems: 'center',
        borderWidth: 0,
        borderColor: '#000'
    }
});

export default styles;